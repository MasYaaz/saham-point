{
  "name": "saham-point",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.1.1",
    "@types/node-cron": "^3.0.11"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "cli": "bun run src/cli.ts",
    "worker": "bun run src/worker.ts",
    "worker:dev": "bun run --watch src/worker.ts",
    "build:tar": "rm -rf dist saham-point.tar,gz && mkdir -p dist && bun build ./src/mcp.ts --outfile ./dist/saham-point-mcp --target bun --external playwright-core --external chromium-bidi && bun build ./src/cli.ts --outfile ./dist/saham-point-cli --target bun --external playwright-core --external chromium-bidi && bun build ./src/worker.ts --outfile ./dist/saham-point-worker --target bun --external playwright-core --external chromium-bidi && chmod +x ./dist/* && tar -czvf saham-point-build.tar.gz -C dist ."
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cheerio": "^1.2.0",
    "node-cron": "^4.6.0",
    "playwright": "^1.62.0",
    "rss-parser": "^3.13.0",
    "technicalindicators": "^3.1.0"
  }
}
