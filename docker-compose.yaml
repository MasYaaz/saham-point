services:
  saham-point:
    build: .
    container_name: saham-point
    restart: unless-stopped
    ports:
      - "80:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    tty: true
